name: GitGoogleContainer
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Login in Docker Registry
        uses: docker/login-action@v1
        with:
          #registry: ghcr.io
          registry: ghcr.io
          #username: ${{ github.repository_owner }}
          username: ${{ github.repository_owner }}
          #password: ${{ secrets.GHCR_IO_TOKEN }}
          password: ${{ secrets.GHCRIOTOKEN }}
      - name: Docker pull and push
        run: |
          docker login ghcr.io -u fanggege123 -p "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"
          docker pull k8s.gcr.io/ingress-nginx/controller:v0.35.0  
          docker tag  k8s.gcr.io/ingress-nginx/controller:v0.35.0  ghcr.io/fanggege123/ingress-nginx/controller:v0.35.0       
          docker push ghcr.io/fanggege123/ingress-nginx/controller:v0.35.0
